<div class="sticky">
  <nav class="top-bar" data-topbar>

    <div class="large-12 small-12 columns">
      <%= form_tag(search_blogs_path, method: :get) do %>
        <div class="row collapse search">
          <div class="large-8 small-8 columns search">
            <%= text_field_tag 'Query', nil, class: '' %>
          </div>
          <div class="large-1 small-1 columns">
            <%= submit_tag 'Search', class: 'search_submit' %>
          </div>
        </div>
      <% end %>
    </div>
  
  </nav>
</div>
